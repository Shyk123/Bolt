/*
import java.io.File

fun replaceCharactersInFile(filePath: String) {
    try {
        // Чтение содержимого файла
        val content = File(filePath).readText()

        // Замена символов '+' на '7'
        val modifiedContent = content.replace('+', '7')

         // Изменение третьего символа на '$'
        val modifiedContent = textNote.substring(0, 2) + '$' + textNote.substring(3)

        // Запись измененного содержимого обратно в файл
        File(filePath).writeText(modifiedContent)

        println("Операция выполнена успешно.")
    } catch (e: Exception) {
        println("Произошла ошибка: ${e.message}")
    }
}






        val conten1 = text.replace('+', '=') // заменяем все + на =
        val conten2 = conten1.replace('-', '=') // заменяем все - на =
        File(nameFile).writeText(conten2)



*/

